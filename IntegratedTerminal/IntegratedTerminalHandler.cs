using MonoDevelop.Components.Commands;

namespace IntegratedTerminal
{
    public class IntegratedTerminalHandler : CommandHandler
    {
        protected override void Run()
        {
            base.Run();
        }

        protected override void Update(CommandInfo info)
        {
            base.Update(info);
        }
    }
}
